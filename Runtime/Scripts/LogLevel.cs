using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace ULogger {
    public enum LogLevel {
        Trace = 0,
        Debug = 1,
        Info = 2,
        Warn = 3,
        Error = 4,
        Fatal = 5
    }
}
